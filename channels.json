{
  "_note": "Schema example. The live file is generated each run by bin/generate_channels_json.sh and uploaded as a Semaphore workflow artifact (not committed back), so digests here stay null. 'latest' = freshly built (api staging / `latest` sidecar); 'stable' = last week's latest, soaked a week (edge / prod / sandbox sidecar).",
  "updated_at": null,
  "images": {
    "prometheus_exporter": {
      "registry": "013323007402.dkr.ecr.eu-west-1.amazonaws.com",
      "versions": {
        "latest": { "latest": null, "stable": null }
      }
    }
  }
}
